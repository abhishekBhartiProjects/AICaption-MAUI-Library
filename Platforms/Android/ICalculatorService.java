package com.mymauilibrary;

public interface ICalculatorService {
    int addNumbers(int a, int b);  // Changed name to avoid conflict
    String getMessageText();       // Changed name to avoid conflict
}